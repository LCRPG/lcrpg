require 'squib'
require 'set'
load 'deck.rb'

task default: [:module]

task :module, [:moduleFolder] do |task, args|
    args.with_defaults(moduleFolder: "");
    buildManifest("cardfiles/#{args[:moduleFolder]}", Set[])
end